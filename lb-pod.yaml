apiVersion: v1
kind: Pod
metadata:
  name: Loadbalancer-pod
  namespace: roboshop
  #labels help to register the pod with service
  labels:
    name: frontend
    environment: dev
    purpose: ALB
spec:
  containers: 
  - name: nginx
    image: nginx:latest